export const ACCESS_KEY = String(process.env.NEXT_PUBLIC_ACCESS_KEY);

export const SECRET_KEY = String(process.env.NEXT_PUBLIC_SECRET_KEY);

export const REGION = String(process.env.NEXT_PUBLIC_REGION);

export const DB_TABLE = String(process.env.NEXT_PUBLIC_DB_TABLE);
